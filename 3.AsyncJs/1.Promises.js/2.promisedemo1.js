function fetchCustom(url) {
    // download content form url
    // download can take sometime
    // how to use it using call back
    /**
     * function fetchCustom(url, fn)
     * we will download the content from url
     * then whatever is the result will pass to 
     * the callback go to 2.1 callbackdemo1.js
     */
}