/**
 * program : if we write a code in file
 * process : program in a running state or a program in execution
 * To build something we need to store values
 * veribles are the bucket in memeory which store values
 * there exist 3 ways to store veriables : let, var, const => these are keywords
 */

let x = 10;
var y = 20;
const z = "abcd";

x = "ab";
console.log(x, y, z);

console.log("b" - "b");
console.log('b' - 'b');
